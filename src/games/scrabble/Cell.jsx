import { useState } from 'react'
function Cell({activeComponent}) {
    // Cell can be empty or contain a tile. Each cell may also have a bonus (dw, tw, dl, tl, start) which changes its color
    return (
        <>
            <h2>Reverse Scrabble</h2>
        </>
    )
}

export default Cell