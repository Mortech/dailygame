import { useState } from 'react'
function ReverseScrabble({activeComponent}) {
    // The tile is a letter and its score - a formerly blank tile has no score
    // The appearance of the tile should change if its cell has a bonus
    return (
        <>
            <h2>Reverse Scrabble</h2>
        </>
    )
}

export default ReverseScrabble