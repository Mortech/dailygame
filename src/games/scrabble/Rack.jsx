import { useState } from 'react'
function Rack({activeComponent}) {
    const [handTiles, setHandTiles] = useState(0)
    // The rack contains the tiles pulled off the board for the move
    return (
        <>
            <h2>Reverse Scrabble</h2>
        </>
    )
}

export default Rack